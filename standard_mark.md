# head
1. dsize = data size;
2. wid = the width of data;
3. hei = the height of data;
4. mod = 
{
  0 = negative sequence;
  1 = positive sequence;
}
5. offset_h = the offset bytes from head to data;
6. offset_* = the offset bytes from head to the newest * core;
7. cont_* = the number of * core;
# data
BYTE x;
BYTE y;
BYTE value_r;
BYTE value_g;
BYTE value_b;